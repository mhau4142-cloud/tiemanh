---
const { class: className = "", ...rest } = Astro.props;
---

<span
  class={`uppercase text-sm font-extrabold tracking-tight ${className}`}
  {...rest}>Tiệm<span class="bg-accent-400 text-black px-1 ml-1">Ảnh</span></span
>
